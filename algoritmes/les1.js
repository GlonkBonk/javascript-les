/*
 * In this assignment you'll need to return the difference between the two
 * array's and put those changes into an array called 'difference'.
 */

var arr_one = ['koen', 'jan', 'peter']
var arr_two = ['koen', 'jan', 'peter', 'diederik', 'thomas']

arr_diff = (arr_one, arr_two) =>{
  var  a = [], diff = [];
  //The rest is for you to find out :)
}

console.log(arr_diff(arr_one, arr_two))

